const user={
    name:"jaheer",
    roll:34
}
//destructuring of object
const{name}=user; //now we can use name directly
console.log(name);
const{roll:r}=user; //to get user roll no now we can use r
console.log(r);
{
    //this is format of a json api ,it can also be in array format
    //randomuser api is an example
}

